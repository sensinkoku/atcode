#include <iostream>
#include <cmath>
#include <algorithm>
#include <iomanip>
using namespace std;
int x_[10], y_[10];
int main() {
	cin.tie(0);
	ios::sync_with_stdio(false);
	
	int x, y, n;
	cin >> x >> y >> n;
	double = 1000;
	for (int i = 0; i< n; ++i) {
		cin >> x_[i] >> y_[i];	
	}



}

